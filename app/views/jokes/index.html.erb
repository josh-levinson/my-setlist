<p style="color: green"><%= notice %></p>

<% content_for :title, "Jokes" %>

<h3>Jokes</h3>

<div id="jokes" class="flex flex-col gap-4">
  <% @jokes.each do |joke| %>
    <div>
      <%= render joke %>
        <p>
          <%= link_to "Show this joke", joke %>
        </p>
    </div>
  <% end %>
</div>

  <%= link_to "New joke", new_joke_path %>
